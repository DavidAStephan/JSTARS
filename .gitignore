# macOS / editor cruft
.DS_Store
.Rhistory

# MCMC/SMC particle-cloud snapshots — large, binary, regenerable via
# jointstar.estimate(...). Keep the derived CSVs/figures/summaries.
jointstar/results/**/particles_stage_*.mat

# throwaway smoke-test output directories
jointstar/results/smoke*/

# source documents (project brief, model transcription, baseline
# parameter table, baseline trend charts) — kept locally, not tracked
fable_project_brief.md
joint_stars_transcription_1.pdf
parameter_estimates_table3.pdf
latent_states.heic
